# Database
DATABASE_URL="postgresql://username@localhost:5432/cartella_clinica"

# Server
PORT=3100
NODE_ENV=development

# JWT
JWT_SECRET=your-secret-key-here
JWT_REFRESH_SECRET=your-refresh-secret-here
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# Redis (opzionale per ora)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# CORS
CORS_ORIGIN=http://localhost:5183

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# Email (configurare più avanti)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@medicinaravenna.it

# Logging
LOG_LEVEL=debug

# GitHub (NON METTERE MAI TOKEN QUI!)
# Usa gh auth login o SSH keys invece
